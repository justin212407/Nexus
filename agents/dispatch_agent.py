from pipeline.state import NexusState


def run_dispatch_agent(state: NexusState) -> dict:
    """Routes TechnicalBrief to Intercom + optional Slack."""
    # TODO: implement — see copilot-instructions.md dispatch_agent section
    return {"dispatched": False}
